# Function to load data files containing bad data for viewing
view_bad_data <- function(rds_file) {
    
    # --> show progress
    
    # --> original rds file name
    
    # --> rds may contain multiple files, prompt to choose one
    
    # --> unzip the file
    
    # --> execute readLines on the file
    
    # --> View head of the resulting list 
    
    # --> Prompt to continue viewing or exit
}

system("ls -al output/rejected_rows*.rds")